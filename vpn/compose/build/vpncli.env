#Vaciamos tabla de enrutamiento y creamos rutas en el archivo /etc/routes, instala iptables-persistent y cliente openvpn
COMMANDS="DEBIAN_FRONTEND=noninteractive apt install -y iptables-persistent openvpn,echo \"ip route del default\">>/etc/routes,echo \"ip route add default via 192.168.202.20\">>/etc/routes"

