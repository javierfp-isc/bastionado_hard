#Vaciamos tabla de enrutamiento y creamos rutas en el archivo /etc/routes
COMMANDS="DEBIAN_FRONTEND=noninteractive apt install -y iptables-persistent,echo \"ip route del default\" >> /etc/routes,echo \"ip route add default via 192.168.203.10\" >> /etc/routes"

