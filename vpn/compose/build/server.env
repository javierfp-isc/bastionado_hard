#Vaciamos tabla de enrutamiento y creamos rutas en el archivo /etc/routes e instalamos openssh-server
COMMANDS="apt install -y openssh-server,echo \"ip route del default\">>/etc/routes,echo \"ip route add default via 192.168.201.10\">>/etc/routes"

